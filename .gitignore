node_modules/
dist/
.next/
out/
*.tsbuildinfo

# Secrets — never commit
.env
.env.*
!.env.example

# Runtime state — data/ledger.jsonl and the dashboard feed are committed
# deliberately by the Sentinel workflow (an audit trail belongs in the repo).
# Everything else under data/ is local scratch state.
.anansi/
data/*
!data/ledger.jsonl
